# Random Text Generation

Random generation of text based on JSON grammar definition.

Currently just hacky console app which generates a random permutation of the specified grammar.

```
bin/console text:generate examples/grammar/mineral.json mineral
```
